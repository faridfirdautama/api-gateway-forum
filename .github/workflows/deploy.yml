name: CI/CD Pipeline Backend Api-gateway ForumApp

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Logging
        run: echo "Start CI/CD Pipeline Backend Api-gateway ForumApp"

      - name: Checkout Main branch
        uses: actions/checkout@v3

      - name: Setup Nodejs
        run: |
          npm install -g pnpm@latest
          pnpm install

      - name: Check code linting
        run: pnpm lint:run

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/forum-app-api-gateway:latest .

      - name: Build Docker Image
        run: docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Push Docker Image to Hub
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/forum-app-api-gateway

      - name: Setup SSH to Server
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.API_GATEWAY_IP }} >> ~/.ssh/known_hosts

        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}

      - name: Deploy to Server
        run: |
          ssh ${{ secrets.SERVER_USERNAME }}@${{ secrets.API_GATEWAY_IP }}"
          cd app/api-gateway-forum && \
          docker pull ${secrets.DOCKERHUB_USERNAME}/forum-app-api-gateway && \
          docker stop forum-app-api-gateway && \
          docker rm forum-app-api-gateway && \
          docker-compose up -d
          "
